local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local MarketPlaceService = game:GetService("MarketplaceService")
local UserInputService = game:GetService("UserInputService")

local LocalPlayer = Players.LocalPlayer
local GhostLib = {}

function GhostLib:CreateWindow()
	local ScreenGui = Instance.new("ScreenGui")
	ScreenGui.Name = "GhostUI_PremiumDark"
	ScreenGui.ResetOnSpawn = false
	ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
	ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

	-- FULL-SCREEN INTRO
	local IntroGui = Instance.new("ScreenGui")
	IntroGui.Name = "GhostIntro_Layer"
	IntroGui.IgnoreGuiInset = true 
	IntroGui.ResetOnSpawn = false
	IntroGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
	IntroGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

	local IntroContainer = Instance.new("Frame")
	IntroContainer.Name = "IntroContainer"
	IntroContainer.Size = UDim2.new(1, 0, 1, 0)
	IntroContainer.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
	IntroContainer.BorderSizePixel = 0
	IntroContainer.ZIndex = 100
	IntroContainer.Parent = IntroGui

	local TextSequenceFrame = Instance.new("Frame")
	TextSequenceFrame.Name = "TextSequenceFrame"
	TextSequenceFrame.Size = UDim2.new(0, 600, 0, 150)
	TextSequenceFrame.Position = UDim2.new(0.5, -300, 0.5, -75)
	TextSequenceFrame.BackgroundTransparency = 1
	TextSequenceFrame.ZIndex = 101
	TextSequenceFrame.Parent = IntroContainer

	local Characters = {"G", "H", "O", "S", "T"}
	local LetterObjects = {}
	local charWidth = 120

	for i, char in ipairs(Characters) do
		local LetterContainer = Instance.new("Frame")
		LetterContainer.Name = "Letter_" .. char
		LetterContainer.Size = UDim2.new(0, charWidth, 1, 0)
		LetterContainer.Position = UDim2.new(0, (i - 1) * charWidth, 0, 0)
		LetterContainer.BackgroundTransparency = 1
		LetterContainer.ZIndex = 102
		LetterContainer.Parent = TextSequenceFrame

		local TextLabel = Instance.new("TextLabel")
		TextLabel.Size = UDim2.new(1, 0, 1, 0)
		TextLabel.BackgroundTransparency = 1
		TextLabel.Text = char
		TextLabel.Font = Enum.Font.Montserrat
		TextLabel.TextSize = 120
		TextLabel.TextColor3 = Color3.fromRGB(245, 245, 245)
		TextLabel.ZIndex = 103
		TextLabel.FontFace = Font.new("rbxasset://fonts/families/Montserrat.json", Enum.FontWeight.Bold)
		TextLabel.Parent = LetterContainer

		local GlowStroke = Instance.new("UIStroke")
		GlowStroke.Thickness = 0
		GlowStroke.Color = Color3.fromRGB(245, 245, 245)
		GlowStroke.Transparency = 1
		GlowStroke.Parent = TextLabel

		table.insert(LetterObjects, {
			Container = LetterContainer,
			Label = TextLabel,
			Stroke = GlowStroke,
			Hit = false
		})
	end

	local Ghost = Instance.new("CanvasGroup")
	Ghost.Name = "Ghost"
	Ghost.Size = UDim2.new(0, 85, 0, 110)
	Ghost.BackgroundTransparency = 0
	Ghost.BackgroundColor3 = Color3.fromRGB(245, 245, 245)
	Ghost.ZIndex = 105 
	Ghost.Parent = TextSequenceFrame
	Ghost.Position = UDim2.new(0, -160, 0.5, -55)
	Instance.new("UICorner", Ghost).CornerRadius = UDim.new(0, 42)

	local Eye1 = Instance.new("Frame")
	Eye1.Size = UDim2.new(0, 9, 0, 13)
	Eye1.Position = UDim2.new(0.32, -4, 0.38, -6)
	Eye1.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
	Eye1.BorderSizePixel = 0
	Eye1.Parent = Ghost
	Instance.new("UICorner", Eye1).CornerRadius = UDim.new(1, 0)

	local Eye2 = Eye1:Clone()
	Eye2.Position = UDim2.new(0.68, -4, 0.38, -6)
	Eye2.Parent = Ghost

	local Mouth = Instance.new("Frame")
	Mouth.Size = UDim2.new(0, 11, 0, 11)
	Mouth.Position = UDim2.new(0.5, -5, 0.58, -5)
	Mouth.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
	Mouth.BorderSizePixel = 0
	Mouth.Parent = Ghost
	Instance.new("UICorner", Mouth).CornerRadius = UDim.new(1, 0)

	local Tentacles = {}
	local waveWidth = 1 / 5
	for i = 1, 5 do
		local wave = Instance.new("Frame")
		wave.Size = UDim2.new(waveWidth, 2, 0, 22)
		wave.Position = UDim2.new(waveWidth * (i - 1), -1, 1, -10)
		wave.BackgroundColor3 = Color3.fromRGB(245, 245, 245)
		wave.BorderSizePixel = 0
		wave.Parent = Ghost
		Instance.new("UICorner", wave).CornerRadius = UDim.new(1, 0)
		if i == 2 or i == 4 then
			wave.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
		end
		table.insert(Tentacles, wave)
	end

	-- MAIN INTERFACE FRAME
	local MainFrame = Instance.new("CanvasGroup")
	MainFrame.Name = "MainFrame"
	MainFrame.Size = UDim2.new(0, 780, 0, 500)
	MainFrame.Position = UDim2.new(0.5, -390, 0.5, -250)
	MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
	MainFrame.BorderSizePixel = 0
	MainFrame.Visible = false 
	MainFrame.Parent = ScreenGui

	local MainCorner = Instance.new("UICorner")
	MainCorner.CornerRadius = UDim.new(0, 12)
	MainCorner.Parent = MainFrame

	local MainStroke = Instance.new("UIStroke")
	MainStroke.Color = Color3.fromRGB(40, 40, 40)
	MainStroke.Thickness = 1
	MainStroke.Parent = MainFrame

	local TopBar = Instance.new("Frame")
	TopBar.Name = "TopBar"
	TopBar.Size = UDim2.new(1, 0, 0, 42)
	TopBar.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
	TopBar.BorderSizePixel = 0
	TopBar.Parent = MainFrame

	local TopBarCorner = Instance.new("UICorner")
	TopBarCorner.CornerRadius = UDim.new(0, 12)
	TopBarCorner.Parent = TopBar

	local TopBarLine = Instance.new("Frame")
	TopBarLine.Size = UDim2.new(1, 0, 0, 1)
	TopBarLine.Position = UDim2.new(0, 0, 1, -1)
	TopBarLine.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
	TopBarLine.BorderSizePixel = 0
	TopBarLine.Parent = TopBar

	local WindowTitle = Instance.new("TextLabel")
	WindowTitle.Name = "WindowTitle"
	WindowTitle.Size = UDim2.new(0, 200, 1, 0)
	WindowTitle.Position = UDim2.new(0, 18, 0, 0)
	WindowTitle.BackgroundTransparency = 1
	WindowTitle.Text = "GHOSTWARE"
	WindowTitle.Font = Enum.Font.Montserrat
	WindowTitle.TextSize = 14
	WindowTitle.TextColor3 = Color3.fromRGB(240, 240, 240)
	WindowTitle.TextXAlignment = Enum.TextXAlignment.Left
	WindowTitle.FontFace = Font.new("rbxasset://fonts/families/Montserrat.json", Enum.FontWeight.Bold)
	WindowTitle.Parent = TopBar

	local WindowControls = Instance.new("Frame")
	WindowControls.Name = "WindowControls"
	WindowControls.Size = UDim2.new(0, 90, 0, 26)
	WindowControls.Position = UDim2.new(1, -98, 0.5, -13)
	WindowControls.BackgroundTransparency = 1
	WindowControls.ZIndex = 10
	WindowControls.Parent = TopBar

	local CloseBtn = Instance.new("TextButton")
	CloseBtn.Name = "CloseBtn"
	CloseBtn.Size = UDim2.new(0, 24, 0, 24)
	CloseBtn.Position = UDim2.new(1, -24, 0.5, -12)
	CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 60, 60)
	CloseBtn.BackgroundTransparency = 0.8
	CloseBtn.Text = "×"
	CloseBtn.Font = Enum.Font.Montserrat
	CloseBtn.TextSize = 18
	CloseBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
	CloseBtn.Parent = WindowControls
	Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 6)

	local MinimizeBtn = Instance.new("TextButton")
	MinimizeBtn.Name = "MinimizeBtn"
	MinimizeBtn.Size = UDim2.new(0, 24, 0, 24)
	MinimizeBtn.Position = UDim2.new(1, -56, 0.5, -12)
	MinimizeBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	MinimizeBtn.BackgroundTransparency = 0.9
	MinimizeBtn.Text = "−"
	MinimizeBtn.Font = Enum.Font.Montserrat
	MinimizeBtn.TextSize = 16
	MinimizeBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
	MinimizeBtn.Parent = WindowControls
	Instance.new("UICorner", MinimizeBtn).CornerRadius = UDim.new(0, 6)

	local IsMinimized = false
	local NormalSize = MainFrame.Size
	
	CloseBtn.MouseButton1Click:Connect(function()
		local closeTween = TweenService:Create(MainFrame, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
			Size = UDim2.new(0, 780, 0, 0),
			GroupTransparency = 1
		})
		closeTween:Play()
		closeTween.Completed:Connect(function()
			ScreenGui:Destroy()
			IntroGui:Destroy()
		end)
	end)

	-- ANIMATED SMOOTH DRAGGING
	local dragging = false
	local dragStart = nil
	local startPos = nil
	local targetPos = MainFrame.Position

	TopBar.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = MainFrame.Position
		end
	end)

	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = false
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			if dragging then
				local delta = input.Position - dragStart
				targetPos = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
			end
		end
	end)

	RunService.RenderStepped:Connect(function(dt)
		if MainFrame.Position ~= targetPos then
			MainFrame.Position = MainFrame.Position:Lerp(targetPos, dt * 12)
		end
	end)

	local introDuration = 4.0
	local ghostTween = TweenService:Create(Ghost, TweenInfo.new(introDuration, Enum.EasingStyle.Linear), {
		Position = UDim2.new(0, 660, 0.5, -55)
	})
	ghostTween:Play()

	local runTime = 0
	local processingConnection
	processingConnection = RunService.Heartbeat:Connect(function(deltaTime)
		runTime = runTime + deltaTime
		local speedScale = 3.2
		local phaseAngle = (runTime * speedScale * math.pi * 2)

		local normalW, normalH = 85, 110
		local deltaWidth = math.sin(phaseAngle) * 14
		local deltaHeight = -math.sin(phaseAngle) * 12

		if Ghost and Ghost:IsDescendantOf(game) then
			Ghost.Size = UDim2.new(0, normalW + deltaWidth, 0, normalH + deltaHeight)
			for positionIdx, tentacleNode in ipairs(Tentacles) do
				local varianceOffset = positionIdx * 0.4
				local calculatedSwing = math.cos(phaseAngle + varianceOffset) * 28
				local dynamicLength = 22 + (math.sin(phaseAngle + varianceOffset) * 9)
				tentacleNode.Rotation = calculatedSwing
				tentacleNode.Size = UDim2.new(waveWidth, 2, 0, dynamicLength)
			end
		end

		local currentGhostCenterX = Ghost.AbsolutePosition.X + (Ghost.AbsoluteSize.X / 2)
		for _, data in ipairs(LetterObjects) do
			if not data.Hit then
				local letterCenterX = data.Container.AbsolutePosition.X + (data.Container.AbsoluteSize.X / 2)
				if currentGhostCenterX >= letterCenterX then
					data.Hit = true
					task.spawn(function()
						local startPositionValue = data.Label.Position
						local layoutFallDropY = startPositionValue + UDim2.new(0, 0, 0, 45)
						TweenService:Create(data.Label, TweenInfo.new(0.35, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
							Position = layoutFallDropY,
							Rotation = 180,
							TextColor3 = Color3.fromRGB(15, 15, 15)
						}):Play()
						TweenService:Create(data.Stroke, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
							Thickness = 3.5,
							Transparency = 0
						}):Play()
						task.wait(0.35)
						TweenService:Create(data.Label, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
							Position = startPositionValue,
							Rotation = 360
						}):Play()
					end)
				end
			end
		end
	end)

	task.delay(introDuration, function()
		if processingConnection then processingConnection:Disconnect() end
		local transitionCanvasFade = TweenService:Create(IntroContainer, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			BackgroundTransparency = 1
		})
		for _, data in ipairs(LetterObjects) do
			TweenService:Create(data.Label, TweenInfo.new(0.35, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { TextTransparency = 1 }):Play()
			TweenService:Create(data.Stroke, TweenInfo.new(0.35, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { Transparency = 1 }):Play()
		end
		TweenService:Create(Ghost, TweenInfo.new(0.35, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { GroupTransparency = 1 }):Play()
		transitionCanvasFade:Play()
		transitionCanvasFade.Completed:Connect(function()
			IntroGui:Destroy()
			MainFrame.Visible = true 
		end)
	end)

	-- SIDEBAR NAVIGATION PANEL
	local LateralFrame = Instance.new("Frame")
	LateralFrame.Name = "LateralFrame"
	LateralFrame.Size = UDim2.new(0, 220, 1, -42)
	LateralFrame.Position = UDim2.new(0, 0, 0, 42)
	LateralFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
	LateralFrame.BorderSizePixel = 0
	LateralFrame.Parent = MainFrame

	local LateralCorner = Instance.new("UICorner")
	LateralCorner.CornerRadius = UDim.new(0, 12)
	LateralCorner.Parent = LateralFrame

	local JointPatch = Instance.new("Frame")
	JointPatch.Size = UDim2.new(0, 20, 0, 20)
	JointPatch.Position = UDim2.new(1, -20, 0, 0)
	JointPatch.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
	JointPatch.BorderSizePixel = 0
	JointPatch.Parent = LateralFrame

	local MainPanelSplitterLine = Instance.new("Frame")
	MainPanelSplitterLine.Size = UDim2.new(0, 1, 1, 0)
	MainPanelSplitterLine.Position = UDim2.new(1, 0, 0, 0)
	MainPanelSplitterLine.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
	MainPanelSplitterLine.BorderSizePixel = 0
	MainPanelSplitterLine.Parent = LateralFrame

	local PlayerImage = Instance.new("ImageLabel")
	PlayerImage.Name = "PlayerImage"
	PlayerImage.Size = UDim2.new(0, 64, 0, 64)
	PlayerImage.Position = UDim2.new(0.5, -32, 0, 24)
	PlayerImage.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
	PlayerImage.Image = Players:GetUserThumbnailAsync(LocalPlayer.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
	PlayerImage.Parent = LateralFrame
	Instance.new("UICorner", PlayerImage).CornerRadius = UDim.new(1, 0)
	Instance.new("UIStroke", PlayerImage).Color = Color3.fromRGB(50, 50, 50)

	local PlayerName = Instance.new("TextLabel")
	PlayerName.Size = UDim2.new(1, -20, 0, 20)
	PlayerName.Position = UDim2.new(0, 10, 0, 98)
	PlayerName.BackgroundTransparency = 1
	PlayerName.Text = "@" .. LocalPlayer.Name
	PlayerName.Font = Enum.Font.Montserrat
	PlayerName.TextSize = 13
	PlayerName.TextColor3 = Color3.fromRGB(200, 200, 200)
	PlayerName.Parent = LateralFrame

	local InfoContainer = Instance.new("Frame")
	InfoContainer.Size = UDim2.new(1, -24, 0, 100)
	InfoContainer.Position = UDim2.new(0, 12, 0, 135)
	InfoContainer.BackgroundTransparency = 1
	InfoContainer.Parent = LateralFrame
	
	local UIListInfo = Instance.new("UIListLayout")
	UIListInfo.Padding = UDim.new(0, 6)
	UIListInfo.SortOrder = Enum.SortOrder.LayoutOrder
	UIListInfo.Parent = InfoContainer

	local function CreateInfoLabel()
		local lbl = Instance.new("TextLabel")
		lbl.Size = UDim2.new(1, 0, 0, 18)
		lbl.BackgroundTransparency = 1
		lbl.Font = Enum.Font.Montserrat
		lbl.TextSize = 12
		lbl.TextColor3 = Color3.fromRGB(150, 150, 150)
		lbl.TextXAlignment = Enum.TextXAlignment.Left
		lbl.TextTruncate = Enum.TextTruncate.AtEnd
		lbl.Parent = InfoContainer
		return lbl
	end

	local GameNameLabel = CreateInfoLabel()
	local PingLabel = CreateInfoLabel()
	local PlayersLabel = CreateInfoLabel()

	task.spawn(function()
		local success, gameInfo = pcall(function()
			return MarketPlaceService:GetProductInfo(game.PlaceId)
		end)
		if success and gameInfo then
			GameNameLabel.Text = "Game: " .. gameInfo.Name
		else
			GameNameLabel.Text = "Game: Workspace Environment"
		end
	end)

	RunService.Heartbeat:Connect(function()
		local networkLatencyValue = math.round(LocalPlayer:GetNetworkPing() * 1000)
		PingLabel.Text = "Ping: " .. networkLatencyValue .. " ms"
		if Players.MaxPlayers > 0 then
			PlayersLabel.Text = "Players: " .. #Players:GetPlayers() .. " / " .. Players.MaxPlayers
		else
			PlayersLabel.Text = "Players: " .. #Players:GetPlayers() .. " / 1"
		end
	end)

	local TabButtonsContainer = Instance.new("ScrollingFrame")
	TabButtonsContainer.Size = UDim2.new(1, -16, 1, -255)
	TabButtonsContainer.Position = UDim2.new(0, 8, 0, 245)
	TabButtonsContainer.BackgroundTransparency = 1
	TabButtonsContainer.CanvasSize = UDim2.new(0, 0, 0, 0)
	TabButtonsContainer.ScrollBarThickness = 0
	TabButtonsContainer.Parent = LateralFrame

	local TabListLayout = Instance.new("UIListLayout")
	TabListLayout.Padding = UDim.new(0, 6)
	TabListLayout.SortOrder = Enum.SortOrder.LayoutOrder
	TabListLayout.Parent = TabButtonsContainer

	-- CONTENT MAIN VIEWPORT ARCHITECTURE
	local ContentFrame = Instance.new("Frame")
	ContentFrame.Name = "ContentFrame"
	ContentFrame.Size = UDim2.new(1, -220, 1, -42)
	ContentFrame.Position = UDim2.new(0, 220, 0, 42)
	ContentFrame.BackgroundTransparency = 1
	ContentFrame.Parent = MainFrame

	-- MINIMIZE LOGIC (Fixed to hide internal frames properly)
	MinimizeBtn.MouseButton1Click:Connect(function()
		IsMinimized = not IsMinimized
		if IsMinimized then
			ContentFrame.Visible = false
			LateralFrame.Visible = false
			local minimizeTween = TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Cubic, Enum.EasingDirection.Out), {
				Size = UDim2.new(0, 780, 0, 42)
			})
			minimizeTween:Play()
			MinimizeBtn.Text = "+"
		else
			local restoreTween = TweenService:Create(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
				Size = NormalSize
			})
			restoreTween:Play()
			restoreTween.Completed:Connect(function()
				ContentFrame.Visible = true
				LateralFrame.Visible = true
			end)
			MinimizeBtn.Text = "−"
		end
	end)

	local TabsFolder = Instance.new("Folder")
	TabsFolder.Name = "Tabs"
	TabsFolder.Parent = ContentFrame

	local isFirstTab = true

	local function CreateElementBase(name, parent, sizeY)
		local base = Instance.new("Frame")
		base.Name = name
		base.Size = UDim2.new(1, 0, 0, sizeY or 46)
		base.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
		base.BorderSizePixel = 0
		base.ClipsDescendants = false
		base.Parent = parent
		Instance.new("UICorner", base).CornerRadius = UDim.new(0, 6)
		
		local s = Instance.new("UIStroke", base)
		s.Color = Color3.fromRGB(45, 45, 45)
		s.Thickness = 1
		
		local label = Instance.new("TextLabel")
		label.Size = UDim2.new(0.5, 0, 0, 46)
		label.Position = UDim2.new(0, 16, 0, 0)
		label.BackgroundTransparency = 1
		label.Text = name
		label.Font = Enum.Font.Montserrat
		label.TextSize = 13
		label.TextColor3 = Color3.fromRGB(220, 220, 220)
		label.TextXAlignment = Enum.TextXAlignment.Left
		label.Parent = base

		return base, label
	end

	-- MODULE GENERATION AND TAB MANAGEMENT
	function GhostLib:CreateTab(tabName)
		local TabButton = Instance.new("TextButton")
		TabButton.Size = UDim2.new(1, 0, 0, 36)
		TabButton.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
		TabButton.Text = tabName
		TabButton.Font = Enum.Font.Montserrat
		TabButton.TextColor3 = Color3.fromRGB(150, 150, 150)
		TabButton.TextSize = 13
		TabButton.Parent = TabButtonsContainer
		Instance.new("UICorner", TabButton).CornerRadius = UDim.new(0, 6)
		
		-- Indicator for better tab appearance
		local TabIndicator = Instance.new("Frame")
		TabIndicator.Size = UDim2.new(0, 3, 0, 0)
		TabIndicator.Position = UDim2.new(0, 0, 0.5, 0)
		TabIndicator.AnchorPoint = Vector2.new(0, 0.5)
		TabIndicator.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
		TabIndicator.BorderSizePixel = 0
		TabIndicator.Parent = TabButton
		Instance.new("UICorner", TabIndicator).CornerRadius = UDim.new(1, 0)

		local TabContent = Instance.new("CanvasGroup") 
		TabContent.Size = UDim2.new(1, -32, 1, -32)
		TabContent.Position = UDim2.new(0, 16, 0, 16)
		TabContent.BackgroundTransparency = 1
		TabContent.Visible = false
		TabContent.GroupTransparency = 0
		TabContent.ClipsDescendants = false
		TabContent.Parent = TabsFolder

		local ContainerScrolling = Instance.new("ScrollingFrame")
		ContainerScrolling.Size = UDim2.new(1, 0, 1, 0)
		ContainerScrolling.BackgroundTransparency = 1
		ContainerScrolling.CanvasSize = UDim2.new(0, 0, 0, 0)
		ContainerScrolling.ScrollBarThickness = 3
		ContainerScrolling.ScrollBarImageColor3 = Color3.fromRGB(60, 60, 60)
		ContainerScrolling.ClipsDescendants = false
		ContainerScrolling.Parent = TabContent

		local ContentLayout = Instance.new("UIListLayout")
		ContentLayout.Padding = UDim.new(0, 8)
		ContentLayout.SortOrder = Enum.SortOrder.LayoutOrder
		ContentLayout.Parent = ContainerScrolling

		ContentLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
			ContainerScrolling.CanvasSize = UDim2.new(0, 0, 0, ContentLayout.AbsoluteContentSize.Y + 15)
		end)

		if isFirstTab then
			TabContent.Visible = true
			TabButton.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
			TabButton.TextColor3 = Color3.fromRGB(255, 255, 255)
			TabIndicator.Size = UDim2.new(0, 3, 0, 18)
			isFirstTab = false
		end

		TabButton.MouseButton1Click:Connect(function()
			for _, child in pairs(TabsFolder:GetChildren()) do
				if child:IsA("CanvasGroup") and child.Visible and child ~= TabContent then
					local fadeOut = TweenService:Create(child, TweenInfo.new(0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
						GroupTransparency = 1
					})
					fadeOut:Play()
					fadeOut.Completed:Connect(function()
						child.Visible = false
					end)
				end
			end
			
			for _, btn in pairs(TabButtonsContainer:GetChildren()) do
				if btn:IsA("TextButton") then
					TweenService:Create(btn, TweenInfo.new(0.2), {
						BackgroundColor3 = Color3.fromRGB(25, 25, 25),
						TextColor3 = Color3.fromRGB(150, 150, 150)
					}):Play()
					TweenService:Create(btn:FindFirstChild("Frame"), TweenInfo.new(0.2), {
						Size = UDim2.new(0, 3, 0, 0)
					}):Play()
				end
			end
			
			TabContent.GroupTransparency = 1
			TabContent.Visible = true
			
			TweenService:Create(TabContent, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
				GroupTransparency = 0
			}):Play()

			TweenService:Create(TabButton, TweenInfo.new(0.2), {
				BackgroundColor3 = Color3.fromRGB(35, 35, 35),
				TextColor3 = Color3.fromRGB(255, 255, 255)
			}):Play()
			TweenService:Create(TabIndicator, TweenInfo.new(0.2), {
				Size = UDim2.new(0, 3, 0, 18)
			}):Play()
		end)

		local ElementCreator = {}

		function ElementCreator:CreateToggle(name, default, callback)
			local enabled = default or false
			local base, lbl = CreateElementBase(name, ContainerScrolling)
			
			local btn = Instance.new("TextButton")
			btn.Size = UDim2.new(0, 44, 0, 22)
			btn.Position = UDim2.new(1, -60, 0.5, -11)
			btn.BackgroundColor3 = enabled and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(50, 50, 50)
			btn.Text = ""
			btn.Parent = base
			Instance.new("UICorner", btn).CornerRadius = UDim.new(1, 0)

			local circle = Instance.new("Frame")
			circle.Size = UDim2.new(0, 16, 0, 16)
			circle.Position = enabled and UDim2.new(1, -20, 0.5, -8) or UDim2.new(0, 4, 0.5, -8)
			circle.BackgroundColor3 = enabled and Color3.fromRGB(20, 20, 20) or Color3.fromRGB(200, 200, 200)
			circle.Parent = btn
			Instance.new("UICorner", circle).CornerRadius = UDim.new(1, 0)

			btn.MouseButton1Click:Connect(function()
				enabled = not enabled
				local targetPos = enabled and UDim2.new(1, -20, 0.5, -8) or UDim2.new(0, 4, 0.5, -8)
				local targetBg = enabled and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(50, 50, 50)
				local targetCircleColor = enabled and Color3.fromRGB(20, 20, 20) or Color3.fromRGB(200, 200, 200)
				
				TweenService:Create(circle, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
					Position = targetPos, 
					BackgroundColor3 = targetCircleColor
				}):Play()
				TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = targetBg}):Play()
				callback(enabled)
			end)
		end

		function ElementCreator:CreateButton(name, callback)
			local base, lbl = CreateElementBase(name, ContainerScrolling)
			lbl.Size = UDim2.new(1, -130, 1, 0)

			local btn = Instance.new("TextButton")
			btn.Size = UDim2.new(0, 100, 0, 30)
			btn.Position = UDim2.new(1, -116, 0.5, -15)
			btn.BackgroundColor3 = Color3.fromRGB(240, 240, 240)
			btn.Font = Enum.Font.Montserrat
			btn.Text = "Execute"
			btn.TextColor3 = Color3.fromRGB(15, 15, 15)
			btn.TextSize = 13
			btn.Parent = base
			Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)

			btn.MouseButton1Click:Connect(function()
				local clickTween = TweenService:Create(btn, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
					Size = UDim2.new(0, 92, 0, 26),
					Position = UDim2.new(1, -112, 0.5, -13)
				})
				clickTween:Play()
				clickTween.Completed:Connect(function()
					TweenService:Create(btn, TweenInfo.new(0.1), {
						Size = UDim2.new(0, 100, 0, 30),
						Position = UDim2.new(1, -116, 0.5, -15)
					}):Play()
				end)
				callback()
			end)
		end

		function ElementCreator:CreateSlider(name, min, max, default, callback)
			local base, lbl = CreateElementBase(name, ContainerScrolling)
			
			local sliderValue = Instance.new("TextLabel")
			sliderValue.Size = UDim2.new(0, 45, 1, 0)
			sliderValue.Position = UDim2.new(1, -61, 0, 0)
			sliderValue.BackgroundTransparency = 1
			sliderValue.Text = tostring(default)
			sliderValue.Font = Enum.Font.Montserrat
			sliderValue.TextSize = 13
			sliderValue.TextColor3 = Color3.fromRGB(180, 180, 180)
			sliderValue.Parent = base

			local slideBar = Instance.new("Frame")
			slideBar.Size = UDim2.new(0, 150, 0, 6)
			slideBar.Position = UDim2.new(1, -220, 0.5, -3)
			slideBar.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
			slideBar.BorderSizePixel = 0
			slideBar.Parent = base
			Instance.new("UICorner", slideBar)

			local fill = Instance.new("Frame")
			fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
			fill.BackgroundColor3 = Color3.fromRGB(240, 240, 240)
			fill.BorderSizePixel = 0
			fill.Parent = slideBar
			Instance.new("UICorner", fill)

			local trigger = Instance.new("TextButton")
			trigger.Size = UDim2.new(1, 0, 1, 0)
			trigger.BackgroundTransparency = 1
			trigger.Text = ""
			trigger.Parent = slideBar

			local function updateSlider()
				local mousePos = UserInputService:GetMouseLocation().X
				local barPos = slideBar.AbsolutePosition.X
				local barWidth = slideBar.AbsoluteSize.X
				local percentage = math.clamp((mousePos - barPos) / barWidth, 0, 1)
				local value = math.round(min + (percentage * (max - min)))
				
				TweenService:Create(fill, TweenInfo.new(0.08, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
					Size = UDim2.new(percentage, 0, 1, 0)
				}):Play()
				
				sliderValue.Text = tostring(value)
				callback(value)
			end

			local sliding = false
			trigger.InputBegan:Connect(function(input)
				if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
					sliding = true
					TweenService:Create(slideBar, TweenInfo.new(0.15), {Size = UDim2.new(0, 150, 0, 8)}):Play()
					updateSlider()
				end
			end)

			UserInputService.InputEnded:Connect(function(input)
				if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
					sliding = false
					TweenService:Create(slideBar, TweenInfo.new(0.15), {Size = UDim2.new(0, 150, 0, 6)}):Play()
				end
			end)

			UserInputService.InputChanged:Connect(function(input)
				if sliding and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
					updateSlider()
				end
			end)
		end

		function ElementCreator:CreateKeybind(name, default, callback)
			local base, lbl = CreateElementBase(name, ContainerScrolling)
			local currentBind = default or Enum.KeyCode.RightControl

			local btn = Instance.new("TextButton")
			btn.Size = UDim2.new(0, 110, 0, 28)
			btn.Position = UDim2.new(1, -126, 0.5, -14)
			btn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
			btn.Font = Enum.Font.Montserrat
			btn.Text = currentBind.Name
			btn.TextColor3 = Color3.fromRGB(220, 220, 220)
			btn.TextSize = 12
			btn.Parent = base
			Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
			
			local bStroke = Instance.new("UIStroke", btn)
			bStroke.Color = Color3.fromRGB(60, 60, 60)

			local listening = false
			btn.MouseButton1Click:Connect(function()
				listening = true
				btn.Text = "..."
				TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(60, 60, 60)}):Play()
			end)

			UserInputService.InputBegan:Connect(function(input, processed)
				if listening and not processed then
					if input.UserInputType == Enum.UserInputType.Keyboard then
						listening = false
						currentBind = input.KeyCode
						btn.Text = currentBind.Name
						TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(45, 45, 45)}):Play()
						callback(currentBind)
					end
				end
			end)
		end

		function ElementCreator:CreateTextBox(name, placeholder, callback)
			local base, lbl = CreateElementBase(name, ContainerScrolling)
			
			local box = Instance.new("TextBox")
			box.Size = UDim2.new(0, 150, 0, 28)
			box.Position = UDim2.new(1, -166, 0.5, -14)
			box.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
			box.Text = ""
			box.PlaceholderText = placeholder
			box.PlaceholderColor3 = Color3.fromRGB(130, 130, 130)
			box.Font = Enum.Font.Montserrat
			box.TextSize = 12
			box.TextColor3 = Color3.fromRGB(220, 220, 220)
			box.Parent = base
			Instance.new("UICorner", box).CornerRadius = UDim.new(0, 6)
			local bStroke = Instance.new("UIStroke", box)
			bStroke.Color = Color3.fromRGB(60, 60, 60)

			box.Focused:Connect(function()
				TweenService:Create(bStroke, TweenInfo.new(0.15), {Color = Color3.fromRGB(200, 200, 200)}):Play()
			end)

			box.FocusLost:Connect(function(enterPressed)
				TweenService:Create(bStroke, TweenInfo.new(0.15), {Color = Color3.fromRGB(60, 60, 60)}):Play()
				callback(box.Text, enterPressed)
			end)
		end

		-- DROPDOWN (Now Uses ScrollingFrame)
		function ElementCreator:CreateDropdown(name, list, callback)
			local base, lbl = CreateElementBase(name, ContainerScrolling, 46)
			local expanded = false

			local btn = Instance.new("TextButton")
			btn.Size = UDim2.new(0, 150, 0, 28)
			btn.Position = UDim2.new(1, -166, 0, 9)
			btn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
			btn.Font = Enum.Font.Montserrat
			btn.Text = "Select..."
			btn.TextColor3 = Color3.fromRGB(200, 200, 200)
			btn.TextSize = 12
			btn.Parent = base
			Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
			local bStroke = Instance.new("UIStroke", btn)
			bStroke.Color = Color3.fromRGB(60, 60, 60)

			-- Fixed to a ScrollingFrame
			local listFrame = Instance.new("ScrollingFrame")
			listFrame.Size = UDim2.new(0, 150, 0, 0)
			listFrame.Position = UDim2.new(1, -166, 0, 42)
			listFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
			listFrame.BorderSizePixel = 0
			listFrame.Visible = false
			listFrame.ClipsDescendants = true
			listFrame.ZIndex = 20
			listFrame.ScrollBarThickness = 2
			listFrame.ScrollBarImageColor3 = Color3.fromRGB(80, 80, 80)
			listFrame.CanvasSize = UDim2.new(0, 0, 0, #list * 28)
			listFrame.Parent = base
			Instance.new("UICorner", listFrame).CornerRadius = UDim.new(0, 6)
			local lStroke = Instance.new("UIStroke", listFrame)
			lStroke.Color = Color3.fromRGB(50, 50, 50)

			local listLayout = Instance.new("UIListLayout")
			listLayout.Parent = listFrame

			for _, item in pairs(list) do
				local itemBtn = Instance.new("TextButton")
				itemBtn.Size = UDim2.new(1, 0, 0, 28)
				itemBtn.BackgroundTransparency = 1
				itemBtn.Text = tostring(item)
				itemBtn.Font = Enum.Font.Montserrat
				itemBtn.TextColor3 = Color3.fromRGB(180, 180, 180)
				itemBtn.TextSize = 12
				itemBtn.ZIndex = 21
				itemBtn.Parent = listFrame

				itemBtn.MouseButton1Click:Connect(function()
					btn.Text = tostring(item)
					expanded = false
					
					local collapseTween = TweenService:Create(listFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.new(0, 150, 0, 0)})
					collapseTween:Play()
					TweenService:Create(base, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.new(1, 0, 0, 46)}):Play()
					
					collapseTween.Completed:Connect(function()
						listFrame.Visible = false
					end)
					callback(item)
				end)
			end

			btn.MouseButton1Click:Connect(function()
				expanded = not expanded
				if expanded then
					listFrame.Visible = true
					local displayHeight = math.min(#list * 28, 112) -- Max 4 items showing
					TweenService:Create(listFrame, TweenInfo.new(0.25, Enum.EasingStyle.Cubic, Enum.EasingDirection.Out), {Size = UDim2.new(0, 150, 0, displayHeight)}):Play()
					TweenService:Create(base, TweenInfo.new(0.25, Enum.EasingStyle.Cubic, Enum.EasingDirection.Out), {Size = UDim2.new(1, 0, 0, 52 + displayHeight)}):Play()
				else
					local collapseTween = TweenService:Create(listFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.new(0, 150, 0, 0)})
					collapseTween:Play()
					TweenService:Create(base, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.new(1, 0, 0, 46)}):Play()
					collapseTween.Completed:Connect(function()
						listFrame.Visible = false
					end)
				end
			end)
		end

		-- MULTI DROPDOWN (Now Uses ScrollingFrame + Optional auto-close parameter)
		function ElementCreator:CreateMultiDropdown(name, list, arg3, arg4)
			local closeOnSelect = false
			local callback = arg4
			
			if type(arg3) == "boolean" then
				closeOnSelect = arg3
			elseif type(arg3) == "function" then
				callback = arg3
			end

			local base, lbl = CreateElementBase(name, ContainerScrolling, 46)
			local expanded = false
			local selectedItems = {}

			local btn = Instance.new("TextButton")
			btn.Size = UDim2.new(0, 150, 0, 28)
			btn.Position = UDim2.new(1, -166, 0, 9)
			btn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
			btn.Font = Enum.Font.Montserrat
			btn.Text = "Multiple..."
			btn.TextColor3 = Color3.fromRGB(200, 200, 200)
			btn.TextSize = 12
			btn.Parent = base
			Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
			local bStroke = Instance.new("UIStroke", btn)
			bStroke.Color = Color3.fromRGB(60, 60, 60)

			local listFrame = Instance.new("ScrollingFrame")
			listFrame.Size = UDim2.new(0, 150, 0, 0)
			listFrame.Position = UDim2.new(1, -166, 0, 42)
			listFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
			listFrame.BorderSizePixel = 0
			listFrame.Visible = false
			listFrame.ClipsDescendants = true
			listFrame.ZIndex = 20
			listFrame.ScrollBarThickness = 2
			listFrame.ScrollBarImageColor3 = Color3.fromRGB(80, 80, 80)
			listFrame.CanvasSize = UDim2.new(0, 0, 0, #list * 28)
			listFrame.Parent = base
			Instance.new("UICorner", listFrame).CornerRadius = UDim.new(0, 6)
			local lStroke = Instance.new("UIStroke", listFrame)
			lStroke.Color = Color3.fromRGB(50, 50, 50)

			local listLayout = Instance.new("UIListLayout")
			listLayout.Parent = listFrame

			for _, item in pairs(list) do
				local itemBtn = Instance.new("TextButton")
				itemBtn.Size = UDim2.new(1, 0, 0, 28)
				itemBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
				itemBtn.BorderSizePixel = 0
				itemBtn.Text = tostring(item)
				itemBtn.Font = Enum.Font.Montserrat
				itemBtn.TextColor3 = Color3.fromRGB(150, 150, 150)
				itemBtn.TextSize = 12
				itemBtn.ZIndex = 21
				itemBtn.Parent = listFrame

				itemBtn.MouseButton1Click:Connect(function()
					if table.find(selectedItems, item) then
						table.remove(selectedItems, table.find(selectedItems, item))
						TweenService:Create(itemBtn, TweenInfo.new(0.15), {TextColor3 = Color3.fromRGB(150, 150, 150), BackgroundColor3 = Color3.fromRGB(35, 35, 35)}):Play()
					else
						table.insert(selectedItems, item)
						TweenService:Create(itemBtn, TweenInfo.new(0.15), {TextColor3 = Color3.fromRGB(240, 240, 240), BackgroundColor3 = Color3.fromRGB(50, 50, 50)}):Play()
					end
					
					if #selectedItems > 0 then
						btn.Text = table.concat(selectedItems, ", ")
					else
						btn.Text = "Multiple..."
					end
					
					if #btn.Text > 16 then
						btn.Text = string.sub(btn.Text, 1, 13) .. "..."
					end
					
					callback(selectedItems)

					-- Auto-Close Option implementation
					if closeOnSelect then
						expanded = false
						local collapseTween = TweenService:Create(listFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.new(0, 150, 0, 0)})
						collapseTween:Play()
						TweenService:Create(base, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.new(1, 0, 0, 46)}):Play()
						collapseTween.Completed:Connect(function()
							listFrame.Visible = false
						end)
					end
				end)
			end

			btn.MouseButton1Click:Connect(function()
				expanded = not expanded
				if expanded then
					listFrame.Visible = true
					local displayHeight = math.min(#list * 28, 112)
					TweenService:Create(listFrame, TweenInfo.new(0.25, Enum.EasingStyle.Cubic, Enum.EasingDirection.Out), {Size = UDim2.new(0, 150, 0, displayHeight)}):Play()
					TweenService:Create(base, TweenInfo.new(0.25, Enum.EasingStyle.Cubic, Enum.EasingDirection.Out), {Size = UDim2.new(1, 0, 0, 52 + displayHeight)}):Play()
				else
					local collapseTween = TweenService:Create(listFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.new(0, 150, 0, 0)})
					collapseTween:Play()
					TweenService:Create(base, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.new(1, 0, 0, 46)}):Play()
					collapseTween.Completed:Connect(function()
						listFrame.Visible = false
					end)
				end
			end)
		end

		function ElementCreator:CreateConfigSystem()
			local base, lbl = CreateElementBase("Configuration Profiles", ContainerScrolling, 60)
			lbl.Size = UDim2.new(1, -200, 1, 0)

			local saveBtn = Instance.new("TextButton")
			saveBtn.Size = UDim2.new(0, 80, 0, 28)
			saveBtn.Position = UDim2.new(1, -186, 0.5, -14)
			saveBtn.BackgroundColor3 = Color3.fromRGB(240, 240, 240)
			saveBtn.Font = Enum.Font.Montserrat
			saveBtn.Text = "Save"
			saveBtn.TextColor3 = Color3.fromRGB(15, 15, 15)
			saveBtn.TextSize = 12
			saveBtn.Parent = base
			Instance.new("UICorner", saveBtn).CornerRadius = UDim.new(0, 6)

			local loadBtn = Instance.new("TextButton")
			loadBtn.Size = UDim2.new(0, 80, 0, 28)
			loadBtn.Position = UDim2.new(1, -96, 0.5, -14)
			loadBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
			loadBtn.Font = Enum.Font.Montserrat
			loadBtn.Text = "Load"
			loadBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
			loadBtn.TextSize = 12
			loadBtn.Parent = base
			Instance.new("UICorner", loadBtn).CornerRadius = UDim.new(0, 6)
			local lStroke = Instance.new("UIStroke", loadBtn)
			lStroke.Color = Color3.fromRGB(60, 60, 60)
		end

		return ElementCreator
	end

	return GhostLib
end

return GhostLib
